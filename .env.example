# 推荐把敏感信息放在 .env，而不是提交到 config.yaml。
FEISHU_WEBHOOK=
FEISHU_SECRET=
# 用于加密保存在本机数据库中的机器人 Webhook；请生成随机值并仅保存在 .env。
NOTIFICATION_ENCRYPTION_KEY=

# 可选：只用于股票池新增时复核行业、同行和研究要点，不生成买卖信号。
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-v4-flash
